{
  "name": "bw",
  "version": "1.0.0",
  "description": "Terminal time tracker for productivity",
  "type": "module",
  "bin": {
    "bw": "./index.ts"
  },
  "scripts": {
    "dev": "bun run index.ts",
    "build": "bun build index.ts --compile --outfile=bw"
  }
}
